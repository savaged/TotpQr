<Window x:Class="TotpQr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TotpQr"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="350" Width="700"
        Loaded="OnWindowLoaded">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top" Margin="0,-2,0,0" TabIndex="99">
            <MenuItem Header="_File">
                <MenuItem Header="_New" Command="{Binding TotpUriDataViewModel.NewCommand}"/>
            </MenuItem>
        </Menu>
        <Grid Margin="20,20,20,30">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <local:TotpUriDataView Grid.Row="0" DataContext="{Binding TotpUriDataViewModel}"/>
            <local:ToptUriDataIndexView Grid.Row="1"/>
        </Grid>
    </DockPanel>
</Window>
