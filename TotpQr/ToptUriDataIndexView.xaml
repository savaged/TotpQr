<UserControl x:Class="TotpQr.ToptUriDataIndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TotpQr"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="OnLoaded">
    <Grid>
        <DataGrid Name="IndexDataGrid"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  ItemsSource="{Binding TotpUriDataIndexViewModel.Index}"
                  SelectedItem="{Binding TotpUriDataViewModel.Model}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Site" Binding="{Binding Site}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Uri" Binding="{Binding Uri}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
